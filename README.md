# 파이썬 프로그래밍 실습 (Python-10)

## 10주차 과제: 딕셔너리 (Dictionary)


-   **과제 #25:** `input().split()`과 `dict(zip())`을 이용한 딕셔너리 생성
-   **과제 #26:** 딕셔너리 키(`[key]`) 접근을 통한 값 출력
-   **과제 #27:** `for`문과 `.keys()`를 이용한 딕셔너리 값 전체 수정
-   **과제 #28:** `in` 연산자로 키 존재 여부 확인 및 `del`을 이용한 삭제
-   **과제 #29:** `for`문과 `.items()`를 이용한 키/값 쌍 전체 출력
-   **과제 #30:** `if` 조건문이 포함된 딕셔너리 컴프리헨션 활용
-   **과제 #31:** `.values()`, `sum()`, `len()`을 이용한 평균 계산
